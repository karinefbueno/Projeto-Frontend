export type PropsButton = {
    link: string,
};

export type PropCard = {
    favorited: [],
};

export type CardProps = {
    id: number,
    titulo: string,
    introducao: string,
    link: string,
    heartEmpty: string,
    heartChecked: string,
    formattedDate: string,
};

export type TypeInfoData = {
    id: number,
    titulo: string,
    introducao: string,
    data_publicacao: string,
    link: string,
    imagens: object,
    image_fulltext: object,
};

export type PropImagem = {
    src: string,
};

